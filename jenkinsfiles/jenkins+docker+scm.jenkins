pipeline {
   agent { 
	dockerfile { 
		dir 'dockerfiles' 
		additionalBuildArgs '-t go-webapp-sample:{BUILD_NUMBER}'
		    } 
	}
    stages {
        stage('') {
            steps {
                echo 'Hello World'
            }
        }
    }
}

