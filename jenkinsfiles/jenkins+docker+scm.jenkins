pipeline {
   agent { 
	dockerfile { 
		dir 'dockerfiles' 
		additionalBuildArgs '-t go-webapp-sample:{BUILD_NUMBER}'
		    } 
	}
    stages {
        stage('Info') {
            steps {
                echo 'Looks like everything works pretty fine (surprisely, isn't it?)'
            }
        }
    }
}

