pipeline {
   agent { dockerfile { dir 'dockerfiles' label 'go-webapp-sample' } }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}

