pipeline {
   agent { 
	dockerfile { 
		dir 'dockerfiles' 
		additionalBuildArgs '-t go-webapp-sample'
		args '-it -p 8080:8080 --rm sh -c 'cd go-webapp-sample && ./go-webapp-sample' '
		    } 
	}
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}

